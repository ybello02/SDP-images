FROM ubuntu:16.04

WORKDIR /App
COPY * /App/

RUN mkdir -p /var/run/sshd
RUN apt update && apt install  openssh-server sudo -y && ssh-keygen -A
ADD ./sshd_config /etc/ssh/sshd_config
RUN echo root:eceUser | chpasswd


CMD ["/usr/sbin/sshd","-D"]
